apiVersion: v1
kind: Service
metadata:
  name: tcc-ossn-app-service
  namespace: app
spec:
  selector:
    app: tcc-ossn-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer